<!--pages/enterprise/detail/detail.wxml-->
<view class="container">
    <view class="statistic" wx:for="{{names}}" wx:for-item="name" data-index="{{index}}" id="type{{index}}">
        <text class="name">{{name}}</text>
        <text class="result">{{result[index]}}</text>
</view>
</view>
